# Loại trừ node_modules để Docker không copy hết thư mục đã cài sẵn trên host
node_modules

# Loại trừ file .env, tránh lộ secret khi build image
.env

# IDE files, log files, folder build không cần thiết
npm-debug.log
yarn-debug.log
yarn-error.log
logs
*.log

# Loại trừ các thư mục ẩn không dùng khi build
.git
.gitignore

# Nếu có thư mục build tạm hoặc file test thì cũng loại
coverage
__tests__
